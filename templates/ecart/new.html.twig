{% extends 'base.html.twig' %}

{% block title %}New Ecart
{% endblock %}

{% block body %}
	{# <h1 class="text-center">Ecarts Calculator</h1> #}

    
		
	

<div class="containerMain fw-bold text-success mt-2">
	<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.douzaine1)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.douzaine1)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Douzaine 1</div>  
		<div class="fs-4 fw-bold text-white text-center rounded-right col-2 {{ ((ecarts.douzaine1)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.douzaine1 }}</div>
	</div>
	<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.douzaine2)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.douzaine2)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Douzaine 2</div>  
		<div class="fs-4 fw-bold text-white text-center rounded-right col-2 {{ ((ecarts.douzaine2)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.douzaine2 }}</div>
	</div>
	<div class="d-flex flex-row border border-{{ ((ecarts.douzaine3)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.douzaine3)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Douzaine 3</div>  
		<div class="fs-4 fw-bold text-white text-center rounded-right col-2 {{ ((ecarts.douzaine3)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.douzaine3 }}</div>
	</div>
</div>

<div class="containerMain fw-bold text-success">
	<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.colonne1)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.colonne1)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Colonne 1</div>  
		<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.colonne1)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.colonne1 }}</div>
	</div>
	<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.colonne2)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.colonne2)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Colonne 2</div>  
		<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.colonne2)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.colonne2 }}</div>
	</div>
	<div class="d-flex flex-row border border-{{ ((ecarts.colonne3)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.colonne3)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Colonne 3</div>  
		<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.colonne3)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.colonne3 }}</div>
	</div>
</div>

{# /// Formulaire #}
<div class="my-1 mx-2 d-flex flex-row text-center">
	<div class="fs-3 mt-2 mx-2 col-8 ">{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class="btn btn-primary my-1 py-1" style="display:none">{{ button_label|default('Save') }}</button>
		{{ form_end(form) }}</div>  
	<div class=" fs-3 fw-bold my-1 mx-3 py-2 text-white text-center col-2 border border-white bg-{{ ecarts.color }} rounded ">{{ ecarts.tirage }}</div>
</div>    

{# <div class="fs-6 text-center fst-italic">Entrez "-1" pour réinitialiser</div> #}
<div class="d-flex flex-row justify-content-between">
	<div class="containerMain col fw-bold text-success">
		<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.sixain1)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain1)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 1</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain1)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain1 }}</div>
		</div>
		<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.sixain2)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain2)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 2</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain2)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain2 }}</div>
		</div>
		<div class="d-flex flex-row border border-{{ ((ecarts.sixain3)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain3)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 3</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain3)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain3 }}</div>
		</div>
	</div>
	<div class="containerMain col fw-bold text-success">
		<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.sixain4)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain4)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 4</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain4)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain4 }}</div>
		</div>
		<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.sixain5)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain5)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 5</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain5)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain5 }}</div>
		</div>
		<div class="d-flex flex-row border border-{{ ((ecarts.sixain6)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain6)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 6</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain6)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain6 }}</div>
		</div>
	</div>
</div>

<div class="d-flex flex-row justify-content-between">
	<div class="containerMain col fw-bold text-success">
		<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.sixain7)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain7)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 7</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain7)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain7 }}</div>
		</div>
		<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.sixain8)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain8)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 8</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain8)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain8 }}</div>
		</div>
		<div class="d-flex flex-row border border-{{ ((ecarts.sixain9)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain9)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 9</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain9)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain9 }}</div>
		</div>
	</div>
	<div class="containerMain col fw-bold text-success">
		<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.sixain10)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain10)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 10</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain10)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain10 }}</div>
		</div>
		<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.sixain11)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain11)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 11</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain11)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain11 }}</div>
		</div>
		<div class="d-flex flex-row border border-{{ ((ecarts.sixain12)>=6) ? 'danger' : 'dark' }} rounded">
			<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.sixain12)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Sixain 12</div>  
			<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.sixain12)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.sixain12 }}</div>
		</div>
	</div>
</div>
{# 
<div class="containerMain fw-bold text-success">
	<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.transversale1)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.transversale1)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Transversale 1</div>  
		<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.transversale1)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.transversale1 }}</div>
	</div>
	<div class="mb-1 d-flex flex-row border border-{{ ((ecarts.transversale2)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.transversale2)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Transversale 2</div>  
		<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.transversale2)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.transversale2 }}</div>
	</div>
	<div class="d-flex flex-row border border-{{ ((ecarts.transversale3)>=6) ? 'danger' : 'dark' }} rounded">
		<div class="backgroundLight fs-4 ps-2 col-10 rounded-left {{ ((ecarts.transversale3)>=6) ? 'bg-danger fw-bold text-white' : '' }}">Transversale 3</div>  
		<div class="fs-4 fw-bold text-white text-center col-2 {{ ((ecarts.transversale3)>=6) ? 'bg-danger' : 'bg-success' }} ">{{ ecarts.transversale3 }}</div>
	</div>
</div> #}

</div>

{# Final 1 : {{ ecarts.final1 }}</br>
Final 2 : {{ ecarts.final2 }}</br>
Final 3 : {{ ecarts.final3 }}</br> #}

				
						{# <a href="{{ path('ecart_show', {'id': ecart.id}) }}">show</a>
						<a href="{{ path('ecart_edit', {'id': ecart.id}) }}">edit</a> #}
				




{% endblock %}
